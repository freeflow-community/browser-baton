{
  "name": "browser-baton",
  "private": true,
  "version": "0.1.0",
  "description": "Session Handoff MVP: hand a browser session from a human's Chrome to a remote agent through a relay",
  "type": "module",
  "workspaces": ["relay", "cli", "testsite", "test"],
  "scripts": {
    "relay": "node relay/server.js",
    "testsite": "node testsite/server.js",
    "test:e2e": "node test/e2e.mjs",
    "check": "node --check relay/server.js && node --check cli/bin/browser-handoff.js && node --check extension/background.js && node --check extension/popup.js && node --check extension/lib/common.js && node --check testsite/server.js && node --check test/agent-sim.mjs && node --check test/e2e.mjs"
  }
}
